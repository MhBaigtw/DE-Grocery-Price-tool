# Handoff — <!-- YYYY-MM-DD HH:MM, agent name -->

> Rewritten in full at the end of every completed section, at the same time as the
> commit. Never written in a hurry at the end of a session — a note written while
> context is running out is exactly the note you cannot trust.
>
> This file records **where work stopped**. It is not a record of what is true about the
> project. Ground truth is the repository: `CLAUDE.md`, the findings docs, the briefs and
> git history. Anyone reading this reads `docs/RESUME.md` first.

## Orientation

Read `docs/RESUME.md` and follow it before doing anything. Do not treat the sections
below as verified state — verify them.

## Current phase and section

<!-- e.g. Phase 4, Section 3 complete. Section 4 not started. -->

## Last commit

<!-- Hash and one-line message. If the tree was not clean when this was written, say so
     explicitly and describe exactly what was uncommitted and why. -->

## What the last session completed

<!-- Three or four lines. Point at the commit and the docs rather than restating their
     contents — a summary that duplicates the findings doc will drift from it. -->

## Immediate next steps

<!-- The specific next task, in enough detail to start without reconstructing intent.
     Name the brief and section it comes from. If the next step needs a decision from
     the project owner, say that instead of guessing. -->

## Open questions and blockers

<!-- Anything waiting on a human: a URL, a credential, a decision, an external reply.
     Distinguish "blocked on a person" from "not started yet" — they are different and
     the next agent will conflate them if you don't. -->

## Known constraints

<!-- Only constraints that arose recently and are not yet in CLAUDE.md. Standing rules
     live in CLAUDE.md, not here. Duplicating them creates two sources that disagree. -->

## Anything the next agent should distrust

<!-- Numbers not yet verified, checks not yet run, work committed but not reviewed,
     assumptions made under time pressure. Be specific. This section is the reason the
     file is worth reading. -->
